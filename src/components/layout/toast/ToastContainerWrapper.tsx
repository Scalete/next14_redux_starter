"use client";

import { FC } from "react";
import { ToastContainer } from "react-toastify";

const ToastContainerWrapper: FC = () => {
  return <ToastContainer position="bottom-right" />;
};

export default ToastContainerWrapper;
